{
  "name": "twizzyxkanban-launcher",
  "private": true,
  "version": "1.0.0",
  "description": "Launcher racine : démarre le backend (qui sert aussi le frontend Angular buildé) en un seul processus. Utilisé pour tester le mode déploiement en local et pour l'hébergement sur un panel type Minestrator.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "postinstall": "npm run build"
  }
}
